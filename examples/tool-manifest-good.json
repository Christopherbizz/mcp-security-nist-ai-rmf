{
  "tools": [
    {
      "name": "read_file",
      "description": "Reads the contents of a file within the allowed workspace directory and returns it as text.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": { "type": "string" }
        },
        "required": ["path"],
        "additionalProperties": false
      }
    }
  ]
}
